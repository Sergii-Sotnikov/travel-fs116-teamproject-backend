# ✈️ Travel Stories API — Auth & Stories Module

## 📋 Опис

Бекенд-модуль для проєкту **Travel Stories**, що відповідає за:

- автентифікацію користувачів (реєстрація, логін, логаут);
- авторизацію через JWT і Google OAuth 2.0;
- CRUD-операції для історій мандрівників (створення, редагування, видалення);
- ресет пароля через email (з токеном підтвердження);
- збереження фото історій у **Cloudinary**.

---

## 🚀 Основний функціонал

### 🔓 Публічні маршрути

| Метод | Ендпоінт                         | Опис                               |
| ----- | -------------------------------- | ---------------------------------- |
| POST  | `/api/auth/register`             | Реєстрація користувача             |
| POST  | `/api/auth/login`                | Логін користувача                  |
| POST  | `/api/auth/refresh`              | Оновлення access токена            |
| POST  | `/api/auth/send-reset-email`     | Надіслати лист для скидання пароля |
| POST  | `/api/auth/reset-password`       | Скидання пароля за токеном         |
| GET   | `/api/auth/google/get-oauth-url` | Отримати Google OAuth URL          |
| POST  | `/api/auth/google/confirm-oauth` | Увійти через Google                |
| GET   | `/api/stories`                   | Отримати всі історії               |
| GET   | `/api/stories/:id`               | Отримати одну історію              |
| GET   | `/api/stories/authors`           | Отримати всіх авторів              |

### 🔒 Приватні маршрути

| Метод  | Ендпоінт           | Опис                  |
| ------ | ------------------ | --------------------- |
| POST   | `/api/auth/logout` | Вихід користувача     |
| POST   | `/api/stories`     | Створити нову історію |
| PATCH  | `/api/stories/:id` | Редагувати історію    |
| DELETE | `/api/stories/:id` | Видалити історію      |

---

## ⚙️ Технології

- **Node.js / Express**
- **MongoDB / Mongoose**
- **JWT + Cookies Auth**
- **Multer + Cloudinary**
- **Joi** для валідації
- **Pino** для логування
- **Nodemailer** для email reset
- **Swagger / Postman Collection** для тестування API

---

## ✅ Статус реалізації

- 🔹 Реєстрація, логін, логаут — **готово**
- 🔹 JWT авторизація з refresh токеном — **готово**
- 🔹 Створення / редагування / видалення історії — **готово**
- 🔹 Завантаження фото на Cloudinary — **готово**
- 🔹 Reset password через email — **готово**
- 🔹 Авторизація через Google — **готово**
- 🔹 Joi-валідація, middleware та логування — **готово**

---

## 🧪 Тестування

Всі основні запити перевірено у **Postman**:

- колекція `travel-api.postman_collection.json` у репозиторії
- токени передаються через **Authorization: Bearer**
- приватні маршрути перевірені з JWT accessToken

---

## 👤 Автор

**Stanislav Tatarchuk**  
Full-Stack Developer / Backend Module — Travel Stories  
📍 Vilnius, Lithuania  
📧 [stasyk55@gmail.com](mailto:stasyk55@gmail.com)

Swagger-опис хто створює коротка специфікація кожного ендпоінта:

POST /api/auth/register — створює користувача
body: { name, email, password }
res: 201 { message, data: user }

POST /api/auth/login — логін
body: { email, password }
res: 200 { data: { accessToken, refreshToken, user } }

GET /api/stories — повертає всі історії (публічний)
GET /api/stories/:id — повертає одну історію
POST /api/stories — створює історію (приватний, form-data)
PATCH /api/stories/:id — оновлює історію
DELETE /api/stories/:id — видаляє історію
